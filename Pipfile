[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pydantic = "==2.11.7"
click = "==8.2.1"

[dev-packages]
pytest = "==8.4.1"
pre-commit = "==4.2.0"

[requires]
python_version = "3.10"

[scripts]
test = "pytest"
install_hooks = "pre-commit install"
lint = "pre-commit run --all-files"
